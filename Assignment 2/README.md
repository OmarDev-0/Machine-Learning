The content here contains Python code that analyzes the Student_Performance.csv dataset. It checks the relationships between different student performance features and applies preprocessing techniques such as handling missing values, detecting outliers, normalization, and PCA using libraries like pandas, numpy, matplotlib, and scikit-learn.
Also used the same csv file in the previous Assignment.
